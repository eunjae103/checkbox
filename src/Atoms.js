import styled from "styled-components";
// import {Button} from './Button';
import { Title } from "./Title";


const Atoms = ({confirmText, onConfirm}) => {
    return (
       <Title size="large">타이틀</Title>
    );
};

export default Atoms;